# ==============================
# = SERVER CONFIG
# ==============================
server.port=8080
spring.application.name=pooja-master-backend-service

# ===============================
# DataSource
# ===============================
spring.datasource.url=jdbc:postgresql://3.111.16.44:5432/mydb
spring.datasource.username=myuser
spring.datasource.password=mypassword
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# Hibernate JPA Settings
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional (recommended for batching)
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true


# Flyway settings
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=true

# ==============================
# = ACTUATOR (optional)
# ==============================
management.endpoints.web.exposure.include=health,info

# ==============================
# = MULTIPART FILE UPLOAD
# ==============================
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# ==============================
# = TWILIO WHATSAPP INTEGRATION
# ==============================
# WARNING: Do not commit actual credentials!
twilio.account.sid=
twilio.auth.token=
twilio.from.phone=whatsapp:+14155238886
twilio.to.phone=whatsapp:+917989583140
